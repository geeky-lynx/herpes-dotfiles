// Zed keymap
//
// For information on binding keys, see the Zed
// documentation: https://zed.dev/docs/key-bindings
//
// To see the default key bindings run `zed: open default keymap`
// from the command palette.
[
  {
    "context": "Workspace",
    "bindings": {
      // "shift shift": "file_finder::Toggle"
    }
  },
  {
    "context": "Editor",
    "bindings": {
      // "j k": ["workspace::SendKeystrokes", "escape"]
      "ctrl-r": "editor::FindAllReferences",
      "ctrl-;": "editor::GoToDefinition",
      "ctrl-shift-;": "editor::GoToDefinitionSplit",
      "ctrl-i": "search::SelectNextMatch",
      "ctrl-shift-i": "search::SelectPrevMatch",
      "ctrl-'": "editor::GoToDiagnostic",
      "ctrl-shift-'": "editor::GoToPrevDiagnostic"
    }
  },
  {
    "context": "Editor && mode == full",
    "bindings": {
      "ctrl-enter": "editor::NewlineBelow",
      "ctrl-shift-enter": "editor::NewlineAbove"
    }
  },
  {
    "context": "Workspace",
    "bindings": {
      "ctrl-k s": "workspace::Save"
    }
  },
  {
    "context": "Workspace",
    "bindings": {
      "ctrl-s": "workspace::SaveWithoutFormat"
    }
  },
  {
    "context": "BufferSearchBar",
    "bindings": {
      "ctrl-alt-h": "search::ToggleReplace"
    }
  }
]

// Good to know default keymaps:
// ctrl + shift + l = select all matches
// ctrl + \ = split screen
// alt + up/down = move line above/below
// shift + alt + up/down = add cursor above/below
// ctrl + shift + alt + up/down = duplicate line to above/below
// ctrl + shift + up/down = select similar match above/below

// Check: what is "hunk"?
// Check: how to select within brackets (like BraSel in VSC, or mi\ma in hx)
